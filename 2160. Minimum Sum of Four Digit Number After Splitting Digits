class Solution {
public:
    int minimumSum(int num) {
        string str= to_string( num);
        sort(str.begin(),str.end());
        int new1=stoi(str.substr(0,1)+str.substr(2,1));
        int new2=stoi(str.substr(1,1)+str.substr(3,1));
        return new1+new2;

        
    }
};
